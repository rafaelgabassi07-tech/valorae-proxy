{
  "name": "nexus-proxy",
  "version": "1.2.0",
  "description": "Proxy serverless para scraping B3 (Investidor10 / StatusInvest / Google News) compatível com Vercel",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "dev": "vercel dev",
    "check": "npm run build && node --check api/asset.js && node --check api/assets.js && node --check api/scrape.js && node --check api/batch-scrape.js && node --check api/news.js && node --check api/sync.js && node --check api/health.js && node --check lib/nexus-engine.js && node --check lib/vercel-runtime.js"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0",
    "tsx": "^4.0.0"
  }
}
