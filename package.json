{
  "name": "nexus-proxy",
  "version": "1.8.4",
  "description": "Proxy serverless para scraping B3 (Investidor10 / StatusInvest / Google News) compatível com Vercel",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node --check api/asset.js && node --check api/assets.js && node --check api/scrape.js && node --check api/sync.js && node --check api/lib/nexus-engine.js",
    "dev": "vercel dev",
    "check": "npm run build"
  },
  "engines": {
    "node": ">=20.0.0"
  },
  "dependencies": {
    "zod": "^3.23.0"
  },
  "devDependencies": {}
}
